# 使用 Node.js 镜像
FROM node:20-alpine

# 安装 pnpm
RUN npm install -g pnpm

# 设置工作目录
WORKDIR /app

# 暴露前端开发端口
EXPOSE 5173

# 默认启动命令：直接运行 pnpm dev 即可（Vite 本身支持热加载）
# 这里需要保证 --host 0.0.0.0 才能从外部访问
CMD ["pnpm", "dev", "--host", "0.0.0.0"]
