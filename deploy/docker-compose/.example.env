# ==============================================
# MCPCan Docker Compose Environment Variables
# ==============================================

# Registry Configuration
# ----------------------------------------------
# Select the registry prefix to use.
# By default, the Global registry is used.
# For China users, uncomment the CN_REGISTRY line and comment out GLOBAL_REGISTRY.

# Option 1: Global Registry (Default)
REGISTRY_PREFIX=77kymo

# Option 2: China Mirror Registry (Uncomment to use)
# REGISTRY_PREFIX=ccr.ccs.tencentyun.com/itqm-private

VERSION=v2.0-mcp-gateway-rebuild

# ENABLED_HTTPS
ENABLED_HTTPS=false

# 域名使用到的路径规则
WEB_PATH_RULE=/
API_PATH_RULE=/api
MCP_GATEWAY_PATH_RULE=/mcp-gateway

# Application Settings
APP_SECRET=MrKuvLNn0hc1kHRbA6EqjSPj1ecrbPDE
RUN_MODE=prod

# Network Configuration
NETWORK_NAME=mcpcan-network

# MySQL Configuration
MYSQL_ROOT_PASSWORD=Hn61Quh3aiJ95D
MYSQL_DATABASE=mcpcan
MYSQL_USER=mcpcan-user
MYSQL_PASSWORD=a6ApqYJIycJJjl
MYSQL_HOST=mysql
MYSQL_PORT=3306
MYSQL_PUBLIC_PORT=31306

# Redis Configuration
REDIS_PASSWORD=dHaARm88OXXN6e
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PUBLIC_PORT=31379

# Storage Configuration (Host Paths)
# Ensure these directories exist or are writable by the docker user
HOST_DATA_PATH=./data/mcpcan
HOST_MYSQL_PATH=./data/mysql
HOST_REDIS_PATH=./data/redis

# Initialization
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123
ADMIN_NICKNAME=admin
ADMIN_ROLE_NAME=admin
ADMIN_ROLE_DESCRIPTION=admin-role
ADMIN_ROLE_LEVEL=1
ADMIN_DATA_SCOPE=all

# mcp-entry
MCP_ENTRY_SERVICE_PORT=80
MCP_ENTRY_SERVICE_HTTPS_PORT=443
MCP_ENTRY_SERVICE_MANAGE_PORT=8080
MCP_ENTRY_SERVICE_NAME=mcp-entry

# mcp-web
MCP_WEB_SERVICE_PORT=3000
MCP_WEB_SERVICE_NAME=mcp-web

# mcp-market
MCP_MARKET_SERVICE_PORT=8080
MCP_MARKET_SERVICE_NAME=mcp-market

# mcp-authz
MCP_AUTHZ_SERVICE_PORT=8081
MCP_AUTHZ_SERVICE_NAME=mcp-authz

# 最大文件上传大小（MB）
MAX_FILE_SIZE=100
ALLOWED_EXTENSIONS=.zip,.tar.gz,.tar,.dxt,.mcpb
