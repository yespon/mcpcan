server:
  httpPort: 8081

secret: "dev-app-secret"

domain: "http://demo.mcp-box.com"

services:
  mcpMarket:
    host: "mcp-market-svc"
    port: 8081
  mcpAuthz:
    host: "127.0.0.1"
    port: 8082

database:
  mysql:
    host: localhost
    port: 31306
    database: mcpcan
    username: mcpcan-user
    password: a6ApqYJIycJJjl
  redis:
    host: localhost
    port: 31379
    password: dHaARm88OXXN6e

log:
  level: debug
  format: text

# 代码包存放路径
code:
  # 上传配置
  upload:
    # 单个文件最大大小 (MB)
    maxFileSize: 100
    # 允许的文件类型
    allowedExtensions: [".zip", ".tar.gz", ".tar", ".rar"]

storage:
  # 存储根目录
  rootPath: ./data
  # 代码包存放路径
  codePath: ./data/code-package
  # OpenAPI 文档存放路径
  openapiPath: ./data/mcpcan/openapi-file
  # static 存放路径
  staticPath: ./data/static
